"""Compiler-visible metadata decorators, also exported directly by :mod:`asmpython`.

Prefer ``from asmpython import Public, access`` in new source.
``asmpython.extras`` is a deprecated alias of this module, kept importable
for compatibility.
"""
from ._api import *
from ._api import __all__
