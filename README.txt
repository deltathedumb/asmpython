mamba compiler - standalone build

This archive contains a completely portable build of the mamba compiler.

Clone this repository to disk, then open a terminal in the repo folder
and run: setx PATH "%CD%;%PATH%"

To verify a correct install, run: mamba from anywhere and if it runs then it worked.

See about.md for more info.
