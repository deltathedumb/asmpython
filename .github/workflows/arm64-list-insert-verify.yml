name: Verify ARM64 list insert

on:
  push:
    branches: [beta]
    paths:
      - "asmpython/_runtime/abi_lists_linux_arm64.S"
      - "asmpython/_backends/arm64/runtime_manifest.py"
      - "asmpython/_backends/arm64/_verify_list_insert.py"
      - "tests/test_arm64_list_insert.py"
      - ".github/workflows/arm64-list-insert-verify.yml"
  pull_request:
    paths:
      - "asmpython/_runtime/abi_lists_linux_arm64.S"
      - "asmpython/_backends/arm64/runtime_manifest.py"
      - "asmpython/_backends/arm64/_verify_list_insert.py"
      - "tests/test_arm64_list_insert.py"
      - ".github/workflows/arm64-list-insert-verify.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cross-verify:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install AArch64 binutils and QEMU
        run: |
          sudo apt-get update
          sudo apt-get install --yes binutils-aarch64-linux-gnu qemu-user
      - name: Run exact lowering and manifest tests
        run: python -m unittest -v tests.test_arm64_list_insert
      - name: Execute list insert through QEMU
        run: python -m asmpython._backends.arm64._verify_list_insert --mode cross

  native-verify:
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install native binutils
        run: |
          sudo apt-get update
          sudo apt-get install --yes binutils
      - name: Run exact lowering and manifest tests
        run: python -m unittest -v tests.test_arm64_list_insert
      - name: Execute list insert natively
        run: python -m asmpython._backends.arm64._verify_list_insert --mode native
