Work on the current 2.0.0 release push. The first 30 CPython official modules pass under pyinbin; the 31-60 slice is 45/60. Do not claim 2.0.0 ready.

Focus on the remaining failures, preferably codec fixture support (`test_codecencodings_*` / `test_codecmaps_*`) and/or `test_build_details`, `test_class` starred class bases, and optional `_testcapi` behavior. Inspect current code before editing. Use apply_patch, run focused conformance tests, commit and push checkpoints, and record findings here. Avoid reverting unrelated work. The primary agent is running a broader conformance slice and will integrate via the shared workspace.
Current status: first 30/30; first 90 modules 61/90 before the latest dynamic-exception fixes. Contextlib sync/async, compile, csv, calendar, cmath, cmd, bz2, bufio, and argparse are now green. Remaining priorities include codec fixture modules, build_details, class starred bases, collections/datetime descriptor binding, and optional native extensions.
Refreshed status: first 90 modules are now 65/90. New remaining failures are unchanged plus optional dbm/lsprof and decimal dynamic exception forms.
Enum/container bootstrap edits are integrated and pushed, but collections/enum still hit a list-descriptor binding defect; continue investigating without reverting the object-model changes.
The 120-module gate reached 83/120 before the latest extension providers; `_testcapi`, `_testinternalcapi`, `_lsprof`, and errno/metadata additions are now pushed and their focused tests pass where applicable.
Latest focused result: `test_collections` and `test_enum` both pass after normalizing host buffered readers in `_MemoryTextIO` and returning complete `nt.stat` results. The attempted 120-module refresh exceeded five minutes due to subprocess accumulation; do not treat it as a new aggregate score.
Conformance checkpoint: `test_build_details`, `test_class`, `test_collections`, and `test_enum` now pass. VM path-like objects bridge `__fspath__`/string methods into host ntpath; class lowering supports starred bases, keyword bases, and custom metaclass `__new__` calls while retaining enum/ABC bootstrap behavior.
Namedtuple constructor checkpoint: `PyClass.__call__` now dispatches user-defined `__new__` (including staticmethod-wrapped definitions), and `super().__new__` falls back to VM allocation through `object.__new__`. The collections namedtuple bootstrap now creates per-type field descriptors and preserves instance values; regression coverage in `tests.test_pyinbin` is 18/18. This removes the `AttributeError: TestResults.skipped` blocker from `test_builtin`; that module now advances to an unrelated bytearray doctest assertion.
Codec fixture task (assigned by this file) is DONE: 31-60 slice confirmed 57/60 at HEAD (commit 69806ade), up from 45/60. Root cause of all 10 test_codecencodings_*/test_codecmaps_* failures was one shared bug chain, not per-codec: (1) `PyClass.__getattr__`'s `_use_args_`/`_member_type_` stubs made `enum._simple_enum` mis-derive `IntEnum`/`StrEnum` construction, so `http/__init__.py`'s `@_simple_enum(IntEnum) class HTTPStatus` (imported transitively via `http.client` -> `test.multibytecodec_support`) never built correctly; (2) a single-plain-Name `ast.Assign` target was routed through the generic multi-target fallback in `frontend.py`, polluting every class body's namespace with spurious `__pyinbin_assign_N` keys that corrupted enum member enumeration; (3) `PyInstance.__getattribute__` exposed its own real (VM-bookkeeping) `__init__` for explicit `instance.__init__(...)` calls, shadowing the emulated class's `__init__` -- `enum.py`'s `_simple_enum` calls `member.__init__(value)` directly, which was silently corrupting `member.cls`; (4) missing `int.__new__`/`str.__new__(cls, value)` handling for classes mixing in a scalar host type, and a missing `_add_member_` stub (real CPython gets it from the `EnumType` metaclass, which the VM never runs). All fixed directly in `pyinbin/vm.py` (`PyClass.__getattr__`, `PyClass.__call__`, `PyInstance.__getattribute__`/`__getattr__`, `_call`'s `__new__` dispatch, `MAKE_CLASS`) and `pyinbin/frontend.py` (`ast.Assign` single-Name fast path). These are general object-model fixes, not per-codec special-casing, so they should also help any other stdlib module that defines a `_simple_enum`-based enum or a plain single-target class-body assignment (this was previously silently broken for ALL classes, not just enums). Remaining 3 failures in the 31-60 slice are unrelated to codecs: `test_build_details` (`Path.startswith` missing), `test_builtin` (starred-value TypeError), `test_class` (starred class bases in `class A(0, *range(1,8), **d): pass` -- sema/frontend never special-cased `Starred` in class base lists). Workspace `tests.runner` 434/459 (25 fails, all in `tests/cases_fail/*`, all "pyinbin fallback masks an expected native-compile-error message") is pre-existing at commit 8f82e6b2 (verified via a throwaway worktree), not a regression from this work.
Checkpoint `7305d4cd` is pushed. It adds module-replacement handling for blocked/fresh imports, `_decimal` native bootstrap metadata, VM code-object introspection, and namedtuple/runtime compatibility. Focused `test_pyinbin` remains 18/18. Current unresolved official slices are `test_decimal` pure `_pydecimal` doctest execution and `test_dis` host disassembly compatibility.
Follow-up focus: `test_embed`, `test_exception_variations`, `test_except_star`, and `test_exceptions` now pass. Exception lowering accepts TryStar and defers invalid exception operands to runtime TypeError; sys integer-limit metadata and __debug__ are populated.
