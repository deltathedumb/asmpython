Work on the current 2.0.0 release push. The first 30 CPython official modules pass under pyinbin; the 31-60 slice is 45/60. Do not claim 2.0.0 ready.

Focus on the remaining failures, preferably codec fixture support (`test_codecencodings_*` / `test_codecmaps_*`) and/or `test_build_details`, `test_class` starred class bases, and optional `_testcapi` behavior. Inspect current code before editing. Use apply_patch, run focused conformance tests, commit and push checkpoints, and record findings here. Avoid reverting unrelated work. The primary agent is running a broader conformance slice and will integrate via the shared workspace.
Current status: first 30/30; first 90 modules 61/90 before the latest dynamic-exception fixes. Contextlib sync/async, compile, csv, calendar, cmath, cmd, bz2, bufio, and argparse are now green. Remaining priorities include codec fixture modules, build_details, class starred bases, collections/datetime descriptor binding, and optional native extensions.
Refreshed status: first 90 modules are now 65/90. New remaining failures are unchanged plus optional dbm/lsprof and decimal dynamic exception forms.
Enum/container bootstrap edits are integrated and pushed, but collections/enum still hit a list-descriptor binding defect; continue investigating without reverting the object-model changes.
The 120-module gate reached 83/120 before the latest extension providers; `_testcapi`, `_testinternalcapi`, `_lsprof`, and errno/metadata additions are now pushed and their focused tests pass where applicable.
Latest focused result: `test_collections` and `test_enum` both pass after normalizing host buffered readers in `_MemoryTextIO` and returning complete `nt.stat` results. The attempted 120-module refresh exceeded five minutes due to subprocess accumulation; do not treat it as a new aggregate score.
